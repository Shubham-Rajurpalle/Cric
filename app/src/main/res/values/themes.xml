<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Cric" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Background Colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Primary Colors -->
        <item name="colorPrimary">#97FD3535</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status Bar Color -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Tab Text Style -->
        <item name="tabTextAppearance">@style/TabTextStyle</item>
    </style>

    <style name="Theme.Cric" parent="Base.Theme.Cric" />

    <style name="TabTextStyle">
        <item name="android:textSize">@dimen/_13sdp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="buttonDetailsTheme" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:backgroundTint">@null</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="CustomAlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="colorPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="title">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowBackground">@drawable/rounded_dialog_background</item>
        <item name="android:windowMinWidthMajor">80%</item> <!-- Controls width in landscape -->
        <item name="android:windowMinWidthMinor">80%</item> <!-- Controls width in portrait -->
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>